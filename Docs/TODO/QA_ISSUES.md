# QA 이슈 로그

## 목적

이 문서는 Notion에서 전달받은 QA 이슈의 처리 상태만 간결하게 기록한다. 사용자는 이슈를 대화로 전달하고, 분류·TODO 등록·완료 이동은 작업자가 담당한다. 장기 구조 개선·리팩터링 backlog는 [미해결 항목](UNRESOLVED_ITEMS.md)에서 관리한다.

## 기본 QA 환경

- 기기: iPhone 12 Pro
- OS: iOS 18.7.8

항목에 환경을 반복해서 쓰지 않는다. 로그인 상태·권한·네트워크처럼 결과에 직접 영향을 준 조건만 필요할 때 짧게 덧붙인다.

## 기록 규칙

- MUST 새 이슈를 `## TODO`에 한 줄 체크박스로 기록한다.
- MUST 해결한 항목을 `## 처리 완료`로 옮기고, 짧은 처리 결과를 남긴다.
- MUST 로그·스크린샷에 token, credential, 정확한 사용자 좌표, 개인 식별 정보를 남기지 않는다.
- SHOULD 화면 경로와 사용자에게 보이는 결과를 먼저 적고, 원인 설명은 필요한 경우에만 덧붙인다.

## TODO

- [ ] 코스 등록 > 튜토리얼 — 좌우 전환이 완료된 시점에만 4pt stepper 단계가 갱신되고, 단계별 뒤로가기·마지막 단계 상단 완료 아이콘이 일치하는지 수동 확인.
- [ ] 코스 등록 > 지도 선택 — 중앙 핀 주소 preview, 출발지 확정 뒤 도착지 전환, 경로 준비 뒤 최종 완료 활성화를 수동 확인.
- [ ] 코스 등록 > 검색 — 행정구역·최근 행정구역 선택 시 지도 이동과 중앙 핀 주소 갱신, 빈 결과 화면을 수동 확인.
- [ ] 홈 > 검색 — 최근 검색·지역 결과의 row divider를 화면 전체 너비로 표시하고, 검색 결과 없음 empty state를 화면 중앙에 배치. 수동 UI 확인 필요.
- [ ] 홈 > 지도·검색 — 검색·마커 선택 뒤 코스/주차장 active marker와 검색창의 선택 장소명이 일치하도록 처리. 수동 확인 필요.
- [ ] 홈 > 검색·바텀시트 — 상세 시트가 열린 상태에서 지역 검색하면 teardown 뒤 추천 목록으로 전환하도록 처리. 수동 확인 필요.
- [ ] 홈 > 코스·주차장 상세 — 하단 행동 바 상단 full-width divider와 10pt 간격을 적용. 수동 UI 확인 필요.
- [ ] 홈 > 추천 목록·코스 상세 — drag indicator를 제외한 상단 앱바를 56pt 고정 높이로 조정. 수동 UI 확인 필요.
- [ ] 홈 > 추천 목록·코스 상세 — non-expanded 시 drag indicator 아래의 불필요한 여백을 제거하고, 확장 코스 상세의 소개·경로 정보 간격을 24pt로 조정. 수동 UI 확인 필요.
- [ ] 코스 등록 > 지도 선택 — 주소 선택 중에도 경유지 삭제가 가능하고, 지도 이동 중 이전 주소를 유지한 뒤 최신 주소로 교체하는지 수동 확인.
- [ ] 코스 등록 > 지도 선택 — 출발지·도착지 경로가 있는 상태에서 경유지를 삭제해도 남은 지점 기준 polyline이 다시 표시되는지 수동 확인.
- [ ] 코스 등록 > 검색 — 최근 검색어가 없을 때 홈 검색과 동일하게 중앙 `최근 검색 내역이 없습니다` empty state가 표시되는지 수동 확인.
- [ ] 홈 > 검색 — 최근 검색어 header와 검색창 간격이 8pt이고, `전체삭제` 표기 및 특별시·광역시 생략 표기가 지역 결과·최근 검색에 일관되게 적용되는지 수동 확인.
- [ ] 홈 > 검색·추천 목록 — 행정구역 선택 뒤 카메라 완료 이벤트 순서와 무관하게 `GET /api/v1/places`가 1회 실행되는지, 빈 배열이면 추천 목록 빈 결과 문구가 시트 중앙에 표시되는지 수동 확인.
- [ ] 앱 전역 > 오류 재시도 — 마이 연습기록·저장목록·내 활동(등록한 코스/작성한 후기)·차단 목록을 포함한 모든 `다시 시도`가 8pt 내부 여백·6pt radius·흰색 body2 텍스트의 primary 채움 공통 버튼으로 표시되는지 수동 확인.
- [ ] 앱 전역 > 네트워크 — 네트워크 오류 문구가 모두 `네트워크 연결을 확인해주세요.`로 표시되는지 수동 확인.
- [ ] 마이 > 저장목록 — 코스·주차장 선택 뒤 Home 검색창의 뒤로가기와 장소명이 상세 시트·선택 마커와 일치하는지 수동 확인.
- [ ] 마이 > 내 활동 — 빈 후기 본문에는 텍스트 컨테이너가 표시되지 않는지 수동 확인.
- [ ] 코스 등록 > 지도 — 연결 단절 시 공통 새로고침 스낵바가 즉시 표시되고, 3초 뒤 지도 대체 화면이 지도 단계에서만 표시되는지 수동 확인.
- [ ] 마이 > 프로필 — 레벨 게이지가 도장 이미지에 비치지 않고, 면허증 카드 그라데이션과 운전 목표 자동 포커스를 수동 확인.
- [ ] 마이 > 설정 — 데이터 출처 화면의 문구·24pt 본문 여백·스크롤을 수동 확인.
- [ ] 마이 > 연습기록·내 활동 — 후기 작성/불가/완료 상태와 목록 드래그 시 드롭다운 닫힘을 수동 확인.
- [ ] 주차장 > 연습 복귀 — GPS 인증·방문 기록 성공 뒤 후기 팝업 없이 연습기록 추가 안내와 Home 갱신만 발생하는지 수동 확인.
- [ ] 앱 전역 > 네트워크 — 연결 단절 시 공통 스낵바를 먼저 표시하고, Home에서만 3초 뒤 지도 대체 화면을 표시하는지 수동 확인.
- [ ] 홈·코스 등록 > 검색 — debounce·페이지 추가 로딩에서 spinner 대신 목록 밀도의 skeleton 행이 표시되는지 수동 확인.
- [ ] 홈 > 지도·GPS — 마지막 성공 위치를 유지하고, 60초 이상 위치 갱신이 실패하면 안내를 한 번만 표시한 뒤 성공 시 재설정되는지 수동 확인.
- [ ] 코스 등록 > 지도 선택 — 경유지 생성 직후에는 `경유지 입력` placeholder를 유지하고, 지도 이동 뒤 중앙 핀 주소로 교체되는지 수동 확인.
- [ ] 코스 등록 > 지도 선택 — 검색창 초기값이 비어 있는지, 출발지 확정 전에는 출발지만, 출발지 확정 뒤에는 도착지, 도착지 확정 뒤에는 경유지를 검색할 수 있는지 수동 확인. 도로 경로 조회 실패 뒤 마지막 선택 지점의 중앙 핀 유지도 함께 확인.
- [ ] 홈 > 추천 목록 필터 — 카테고리 선택 시에만 해당 연습유형이 표시되고, 같은 카테고리를 다시 탭하면 연습유형 영역만 접히는지 확인. 카테고리를 오가도 누적 연습유형·주차 필터가 유지되어야 함.
- [ ] 코스 등록 > 상세 입력 — 서버 정렬 첫 카테고리(현재 기초 주행)가 기본 선택되고 해당 연습유형만 표시되는지, 카테고리 전환 뒤 이전 유형 선택이 유지되는지 확인. 카테고리·연습유형·한줄 소개의 primary 필수 `*` 표기도 확인.
- [ ] 코스 등록 > 핀 수정 — 출발지·경유지·도착지 선택을 확정하기 전에는 `완료`가 비활성이고, 확정 뒤에만 저장할 수 있는지 수동 확인.
- [ ] 코스 등록 > 상세 입력 — 한줄 소개는 첫 글자 입력 뒤 10자 미만이면 빨간 테두리·길이 안내·글자 수를 표시하고, 다시 0자로 지워도 안내 상태가 유지되는지 수동 확인.
- [ ] 홈 > 바텀시트 — 추천 목록·코스·주차장 시트에서 indicator와 제목 영역 모두 수직 drag로 확장·축소·닫기가 되는지 확인. 필터·닫기·북마크·목록 터치는 기존대로 동작해야 함.
- [ ] 홈 > 추천 목록 빈 결과 — indicator 아래 48pt 투명 영역을 잡고 수직 drag로 확장·축소·닫을 수 있는지 확인. 빈 결과 문구·개발용 이미지 탭·필터 동작은 유지되어야 함.
- [ ] 홈 > 지도·추천 목록 — 코스 마커 선택 시 polyline이 지도 영역을 충분히 채우는지, 추천 목록에서 코스·주차장을 선택하면 검색창에 뒤로가기와 장소명이 즉시 표시되는지 수동 확인.
- [ ] 홈 > 둘러보기 지도 — 단일 코스·주차장 마커 탭은 지도 이동 없이 로그인 안내만 표시하고, 클러스터 마커는 기존 확대 동작을 유지하는지 수동 확인.
- [ ] 홈·코스 등록 > 지도 경로 — 모든 지도 route polyline에 `#2600B1` 외곽선이 일관되게 표시되는지 수동 확인.
- [ ] 코스 > 연습 복귀 — GPS 인증·방문 기록 성공 시점에만 후기 팝업이 표시되고, 인증된 코스에서 `X`·`안 했어요`를 눌러도 연습기록이 유지되는지 수동 확인.
- [ ] 코스·주차장 > 측정 종료 — Prod는 GPS 인증 전 `측정 종료` 시 POST·후기 권유 없이 세션만 정리하는지, Dev 코스는 외부 앱 5초 이상 체류 뒤 복귀 시 테스트용 POST·후기 권유가 표시되는지 수동 확인.
- [ ] Dev > 연습기록 — 주차 유형을 포함한 코스도 `후기 작성`, 주차장(`practiceTypes = [PARKING]`)만 `작성 불가`로 표시되는지 수동 확인.
- [ ] Live Activity > 카드 탭 — 앱 강제 종료 뒤 진행 중·완료 카드를 탭하면 해당 Live Activity만 즉시 사라지고, 앱은 정상 진입하는지 수동 확인.
- [ ] Live Activity > Dynamic Island — 이동 중·주행 중·완료 확장 상태에서 상·하·좌·우 잘림 없이 표시되고, compact·minimal 및 잠금 화면 카드가 기존대로 유지되는지 수동 확인.
- [ ] 홈 > 지도·추천 목록 — 지도 viewport를 사용자 조작으로 이동한 뒤 재검색이 필요한 상태인데도 `재검색` 버튼이 보이지 않을 수 있음. 로딩 중 이동·프로그램 카메라 이동 직후 사용자 이동을 우선 수정하고, iOS 16.1~26 실기기에서 확인.

## QA-20260830-01 — 홈 지도 이동 뒤 재검색 버튼 누락

- 상태: Fixed · 실기기 검증 대기 · 우선순위: P0
- 화면 경로: 홈 > 지도 > 추천 목록
- 재현:
  1. 추천 목록의 초기 조회·수동 재검색·다음 페이지 조회 중 하나가 진행 중인 상태에서 지도를 드래그하거나 확대/축소한다.
  2. 지도 이동이 끝난 뒤 화면 상단의 `재검색` 버튼 노출을 확인한다.
  3. 마커 선택·검색 결과 선택 등 프로그램 카메라 이동 직후 사용자가 지도를 드래그하는 흐름도 반복한다.
- 기대 결과: 현재 목록의 viewport와 사용자가 보고 있는 viewport가 달라져 다시 조회가 필요하면, 목록 요청 상태와 무관하게 `재검색` 버튼이 표시된다.
- 실제 결과: 일부 기기 또는 이벤트 순서에서 버튼이 표시되지 않아 현재 지도 범위로 목록을 다시 조회할 진입점이 사라진다.
- 확정 원인 및 처리:
  - `RecommendListBottomSheetReducer.viewportChanged`는 로딩 중 사용자 이동에서 `needsResearch = true`를 설정한 뒤 요청을 취소하고 즉시 반환했다. 이 경로는 `displayStateEffect`를 호출하지 않아 `HomeMapState.isResearchButtonVisible`에 최신 값이 전달되지 않았다.
  - 해당 경로는 이제 버튼 표시 상태 delegate를 먼저 전달하고, 다음 reducer action에서 기존 목록 요청을 취소한다. request revision 방어는 유지한다.
  - Kakao Map adapter는 `cameraStopped` 콜백 한 번과 최대 2초의 프로그램 카메라 이동 표식으로 사용자 이동을 판별한다. 프로그램 이동 완료와 사용자 드래그의 이벤트 순서가 바뀌면 사용자 이동을 프로그램 이동으로 분류할 위험이 있다.
- 수정 방향:
  - 취소 경로에서도 `needsResearch` 변경을 Map 표시 상태에 즉시 전달하고, 취소 뒤 늦은 응답은 기존 revision으로 계속 차단한다.
  - 프로그램 카메라 이동의 완료 식별자를 콜백과 명시적으로 연결하고, 사용자 gesture가 이를 덮어쓰는 경우를 별도 처리한다. 시간 기반 2초 표식만을 판별 근거로 사용하지 않는다.
  - 수정 전후 `isUserInitiated`, `needsResearch`, 표시 상태 전파를 개인 위치 정보 없이 개발 로그로 확인한다.
- 검증:
  - reducer 회귀 테스트: 초기 조회·수동 재검색·다음 페이지 로딩 중 viewport 이동 시 `needsResearch`와 버튼 표시 delegate가 함께 갱신되는지 확인한다.
  - 실기기: iOS 16.1, 17, 18, 26에서 초기 로딩·수동 재검색·마커/검색 카메라 이동 직후 드래그·빠른 연속 확대/축소·백그라운드 복귀 뒤 드래그를 확인한다.
  - 클릭 후 현재 viewport의 목록 요청이 1회만 시작되고, 취소된 이전 응답이 목록을 덮어쓰지 않는지 확인한다.

## 처리 완료

- [x] 홈 > 지도·위치 — 지도 엔진 준비 전 누락된 내 위치 마커를 재동기화하고, 초기 렌더 뒤 한 번 재시도하도록 처리.
- [x] 홈 > 지도·위치 — Home 활성·권한 허용 상태에서 60초마다 위치를 갱신하고, 주기 갱신에서는 카메라를 이동하지 않도록 처리.
- [x] 온보딩 > 운전 경험 — Q1 선택 시 Q2와 Q3을 함께 표시하도록 처리.
- [x] 홈 > 지도 — 코스 마커 선택 시 코스 전용 카메라 확대 레벨로 이동하도록 처리.
- [x] 홈 > 검색 — 최근 검색어가 비어 있으면 화면 중앙에 `최근 검색 내역이 없습니다` empty state를 표시하도록 처리.
- [x] 홈 > 검색 — 결과가 없는 키워드는 추천 목록 문구 대신 `HomeSearchEmptyState`를 표시하도록 처리.
- [x] 홈 > 검색 — 지역 후보는 `/api/v1/places/related-search` 응답 원문을 표시함을 확인. `korean_administrative_areas.json`은 코스 등록 검색에서만 사용.
